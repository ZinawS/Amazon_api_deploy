{
  "name": "amazon_api",
  "version": "1.0.0",
  "description": "Amazon-style API backend with Express, Stripe, and Firestore",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "prestart": "npm install",
    "build": "npm install",  
    "dev": "nodemon index.js",
    "format": "prettier --write .",
    "test": "echo \"No test specified\" && exit 0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.21.0",
    "firebase": "^11.8.1",
    "stripe": "^18.2.0",
    "helmet": "^7.1.0"  
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "prettier": "^3.2.5"
  },
  "engines": {
    "node": "18.x"  
  }
}